# ForgeGuard fork base
# Schema: .forgeguard/schemas/fork-upstream-base.schema.json
#
# Records which upstream state this fork's default branch mirrors. ForgeGuard
# publishes no build artifacts for this repository, and this fork is NOT a
# release channel -- use an upstream release for anything you intend to run.
# The base commit is untagged: an upstream development commit.
---
$schema: ./.forgeguard/schemas/fork-upstream-base.schema.json
version: 1

upstream:
  repository: "vllm-project/vllm-omni"
  url: "https://github.com/vllm-project/vllm-omni"
  license: "Apache-2.0"

base:
  kind: "branch-snapshot"
  branch: "main"
  commit: "9f3a73df17757eb9c198b4ce230f77ed092439f4"
  tag_at_commit: null
  recorded_on: "2026-07-27"

forgeguard:
  revision: 1
  publishes_artifacts: false

delta_inventory: "docs/site/fork/forgeguard-changes.md"
